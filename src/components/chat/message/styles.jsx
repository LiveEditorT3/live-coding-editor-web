import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  bubble: {
    padding: "10px",
    borderRadius: "25px",
    maxWidth: "85%",
    margin: "10px"
  },
  text: {
    wordWrap: "break-word"
  }
}));